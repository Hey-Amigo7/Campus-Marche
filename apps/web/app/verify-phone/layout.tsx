import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Verify Phone",
  description: "Link and verify your Ghana phone number on Campus Marche.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
